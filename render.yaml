services:
  - type: web
    name: yield-vision-aoi
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 --threads 10 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
